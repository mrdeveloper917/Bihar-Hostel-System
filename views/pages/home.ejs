<% title = "Bihar Polytechnic Hostel Management System" %>

<!-- ========================= HERO SECTION ========================= -->
<section
  class="hero-section position-relative d-flex flex-column align-items-center justify-content-center text-white text-center overflow-hidden"
>
  <div class="hero-bg"></div>
  <canvas id="heroParticles"></canvas>

  <div class="container position-relative z-2">
    <div
      class="glass-hero p-5 rounded-4 shadow-lg animate__animated animate__fadeInUp"
    >
      <img
        src="/img/logo.png"
        alt="Bihar Govt Logo"
        width="90"
        class="mb-3 opacity-90"
      />

      <h1 class="display-5 fw-bold mb-3 hero-heading gradient-text">
        Bihar Polytechnic <br />
        Hostel Management System
      </h1>

      <p class="lead mb-4 text-light opacity-90 col-lg-8 mx-auto">
        A Government Initiative for
        <strong>Digital, Transparent & Smart Hostel Management</strong> —
        simplifying student life across all Polytechnic Colleges.
      </p>

      <div class="d-flex justify-content-center flex-wrap gap-3">
        <a
          href="/register"
          class="btn btn-glow btn-lg rounded-pill px-5 shadow"
        >
          <i class="bi bi-person-plus me-2"></i> Get Started
        </a>
        <a href="/login" class="btn btn-outline-light btn-lg rounded-pill px-5">
          <i class="bi bi-box-arrow-in-right me-2"></i> Login
        </a>
      </div>

      <div class="mt-4 text-light-50 small fw-light">
        <i class="bi bi-geo-alt"></i> Department of Science & Technology,
        Government of Bihar
      </div>
    </div>
  </div>
</section>

<!-- ========================= NOTICE SECTION ========================= -->
<section class="notice-bar py-2 text-white text-center">
  <marquee behavior="scroll" direction="left" scrollamount="6">
    📰 Latest Notice: Hostel fee payment for Semester 5 is now open • New room
    allotment results declared • Maintenance portal open for students • Visitors
    tracking now digitalized • Check your leave approval status online
  </marquee>
</section>

<!-- ========================= ABOUT SECTION ========================= -->
<section class="about-section py-5 bg-light text-center">
  <div class="container animate__animated animate__fadeIn">
    <h2 class="fw-bold text-gradient mb-3">Our Vision & Mission</h2>
    <p class="text-muted mb-5 col-lg-8 mx-auto">
      To make every hostel across Bihar’s Polytechnic colleges fully digital,
      fair, and transparent — bringing smart automation, student convenience,
      and accountability.
    </p>

    <div class="row g-4 justify-content-center">
      <div class="col-md-4">
        <div class="info-card p-4 shadow-glass h-100">
          <div class="icon-circle bg-gradient-primary mb-3">
            <i class="bi bi-cpu fs-3 text-white"></i>
          </div>
          <h6 class="fw-semibold mb-2">Digital Governance</h6>
          <p class="small text-muted">
            All hostel processes — allotment, fee, and complaints — integrated
            under one digital platform.
          </p>
        </div>
      </div>
      <div class="col-md-4">
        <div class="info-card p-4 shadow-glass h-100">
          <div class="icon-circle bg-gradient-success mb-3">
            <i class="bi bi-shield-check fs-3 text-white"></i>
          </div>
          <h6 class="fw-semibold mb-2">Transparency & Safety</h6>
          <p class="small text-muted">
            Students get clear, trackable updates; admins maintain integrity and
            accountability.
          </p>
        </div>
      </div>
      <div class="col-md-4">
        <div class="info-card p-4 shadow-glass h-100">
          <div class="icon-circle bg-gradient-warning mb-3">
            <i class="bi bi-lightning-charge fs-3 text-white"></i>
          </div>
          <h6 class="fw-semibold mb-2">Efficiency & Innovation</h6>
          <p class="small text-muted">
            Cutting-edge tools for hostel automation with minimal paperwork.
          </p>
        </div>
      </div>
    </div>
  </div>
</section>

<!-- ========================= FEATURES SECTION ========================= -->
<section class="features-section py-5 text-center">
  <div class="container animate__animated animate__fadeInUp">
    <h2 class="fw-bold text-gradient mb-4">Smart Digital Features</h2>
    <div class="row g-4">
      <div class="col-md-4">
        <div class="feature-card p-4 shadow-glass h-100">
          <i class="bi bi-door-open fs-2 mb-3 text-primary"></i>
          <h6 class="fw-semibold">Room Allotment</h6>
          <p class="small text-muted">
            Smart & automatic allotment of rooms with vacancy and preference
            tracking.
          </p>
        </div>
      </div>
      <div class="col-md-4">
        <div class="feature-card p-4 shadow-glass h-100">
          <i class="bi bi-wallet2 fs-2 mb-3 text-success"></i>
          <h6 class="fw-semibold">Online Fee Payment</h6>
          <p class="small text-muted">
            Razorpay & Paytm integration for secure hostel payments with
            e-receipts.
          </p>
        </div>
      </div>
      <div class="col-md-4">
        <div class="feature-card p-4 shadow-glass h-100">
          <i class="bi bi-chat-left-text fs-2 mb-3 text-warning"></i>
          <h6 class="fw-semibold">Complaint & Maintenance</h6>
          <p class="small text-muted">
            Raise complaints and track their status directly from your student
            dashboard.
          </p>
        </div>
      </div>
    </div>
  </div>
</section>

<!-- ========================= GALLERY SECTION ========================= -->
<section class="gallery-section py-5 bg-light text-center">
  <div class="container">
    <h2 class="fw-bold text-gradient mb-4">Campus Glimpse</h2>
    <div class="row g-3">
      <div class="col-md-4">
        <img
          src="/img/hostel1.jpg"
          class="img-fluid rounded-4 shadow"
          alt="Hostel Block"
        />
      </div>
      <div class="col-md-4">
        <img
          src="/img/hostel2.jpg"
          class="img-fluid rounded-4 shadow"
          alt="Campus Area"
        />
      </div>
      <div class="col-md-4">
        <img
          src="/img/hostel3.jpg"
          class="img-fluid rounded-4 shadow"
          alt="Student Mess"
        />
      </div>
    </div>
  </div>
</section>

<!-- ========================= TESTIMONIALS ========================= -->
<section class="testimonials-section py-5 text-center">
  <div class="container">
    <h2 class="fw-bold text-gradient mb-4">What Students Say</h2>
    <div id="carouselTestimonials" class="carousel slide">
      <div class="carousel-inner">
        <div class="carousel-item active">
          <blockquote class="blockquote">
            “This system made our hostel management faster, transparent, and
            stress-free.”
          </blockquote>
          <img src="/img/avatar.png" width="60" class="rounded-circle mt-3" />
          <h6 class="mt-2 fw-semibold">Anjali Kumari</h6>
          <p class="text-muted small">Patna Polytechnic</p>
        </div>
        <div class="carousel-item">
          <blockquote class="blockquote">
            “Online payment and complaint tracking have made hostel life simpler
            than ever.”
          </blockquote>
          <img src="/img/avatar.png" width="60" class="rounded-circle mt-3" />
          <h6 class="mt-2 fw-semibold">Ravi Kumar</h6>
          <p class="text-muted small">Muzaffarpur Polytechnic</p>
        </div>
      </div>
      <button
        class="carousel-control-prev"
        type="button"
        data-bs-target="#carouselTestimonials"
        data-bs-slide="prev"
      >
        <span
          class="carousel-control-prev-icon bg-dark rounded-circle p-3"
        ></span>
      </button>
      <button
        class="carousel-control-next"
        type="button"
        data-bs-target="#carouselTestimonials"
        data-bs-slide="next"
      >
        <span
          class="carousel-control-next-icon bg-dark rounded-circle p-3"
        ></span>
      </button>
    </div>
  </div>
</section>

<!-- ========================= CTA SECTION ========================= -->
<section class="cta-section text-center text-white py-5">
  <div class="container animate__animated animate__fadeInUp">
    <h2 class="fw-bold mb-3">Join Bihar’s Digital Hostel Revolution</h2>
    <p class="lead mb-4 opacity-90">
      Empowering every Polytechnic student with a smarter, safer, and more
      transparent hostel experience.
    </p>
    <a
      href="/register"
      class="btn btn-light btn-lg rounded-pill px-5 fw-semibold shadow"
    >
      <i class="bi bi-rocket-takeoff me-2"></i> Register Now
    </a>
  </div>
</section>

<!-- ========================= STYLES ========================= -->
<style>
  body {
    font-family: "Poppins", sans-serif;
    background: #f9fbff;
    overflow-x: hidden;
  }

  /* ========================= ULTRA HERO ========================= */
  .hero-section {
    position: relative;
    min-height: 100vh;
    color: #fff;
    background: linear-gradient(135deg, #007bff, #6610f2, #00c6ff);
    background-size: 400% 400%;
    animation: gradientShift 12s ease infinite;
    overflow: hidden;
  }

  @keyframes gradientShift {
    0% {
      background-position: 0% 50%;
    }
    50% {
      background-position: 100% 50%;
    }
    100% {
      background-position: 0% 50%;
    }
  }

  .hero-bg::before {
    content: "";
    position: absolute;
    inset: 0;
    background-image: linear-gradient(
        90deg,
        rgba(255, 255, 255, 0.05) 1px,
        transparent 1px
      ),
      linear-gradient(rgba(255, 255, 255, 0.05) 1px, transparent 1px);
    background-size: 80px 80px;
    z-index: 0;
    animation: gridMove 40s linear infinite;
  }

  @keyframes gridMove {
    0% {
      background-position: 0 0, 0 0;
    }
    100% {
      background-position: 80px 80px, 80px 80px;
    }
  }

  .glass-hero {
    background: rgba(255, 255, 255, 0.08);
    border: 1px solid rgba(255, 255, 255, 0.25);
    backdrop-filter: blur(15px);
    border-radius: 25px;
    box-shadow: 0 15px 40px rgba(0, 0, 0, 0.3);
    transition: all 0.5s ease;
  }

  .glass-hero:hover {
    transform: translateY(-5px);
    box-shadow: 0 25px 50px rgba(0, 0, 0, 0.4);
  }

  .gradient-text {
    background: linear-gradient(90deg, #00e1ff, #5b8cff, #9f6bff);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    animation: textGlow 5s ease infinite;
  }

  @keyframes textGlow {
    0%,
    100% {
      text-shadow: 0 0 20px rgba(0, 136, 255, 0.4);
    }
    50% {
      text-shadow: 0 0 30px rgba(255, 255, 255, 0.6);
    }
  }

  .btn-glow {
    background: linear-gradient(135deg, #00c6ff, #0072ff);
    color: #fff;
    border: none;
    box-shadow: 0 0 20px rgba(0, 140, 255, 0.4);
    transition: all 0.3s ease;
  }

  .btn-glow:hover {
    transform: translateY(-3px) scale(1.05);
    box-shadow: 0 0 30px rgba(0, 140, 255, 0.6);
  }

  #heroParticles {
    position: absolute;
    inset: 0;
    z-index: 0;
    opacity: 0.7;
  }

  .notice-bar {
    background: linear-gradient(90deg, #007bff, #6610f2);
    font-weight: 500;
    letter-spacing: 0.5px;
  }

  .icon-circle {
    width: 60px;
    height: 60px;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .bg-gradient-primary {
    background: linear-gradient(135deg, #007bff, #6610f2);
  }
  .bg-gradient-success {
    background: linear-gradient(135deg, #28a745, #20c997);
  }
  .bg-gradient-warning {
    background: linear-gradient(135deg, #ffc107, #ff8c00);
  }

  .shadow-glass {
    background: rgba(255, 255, 255, 0.95);
    border-radius: 18px;
    backdrop-filter: blur(10px);
    transition: transform 0.3s, box-shadow 0.3s;
  }

  .shadow-glass:hover {
    transform: translateY(-6px);
    box-shadow: 0 15px 35px rgba(0, 0, 0, 0.15);
  }

  .cta-section {
    background: linear-gradient(135deg, #007bff, #6610f2);
  }

  @media (max-width: 768px) {
    .glass-hero {
      padding: 2rem;
    }
    .hero-heading {
      font-size: 2rem;
    }
    .btn-lg {
      width: 100%;
    }
  }
</style>

<!-- ========================= PARTICLES SCRIPT ========================= -->
<script>
  const canvas = document.getElementById("heroParticles");
  const ctx = canvas.getContext("2d");
  let particles = [];

  canvas.width = innerWidth;
  canvas.height = innerHeight;

  class Particle {
    constructor() {
      this.x = Math.random() * canvas.width;
      this.y = Math.random() * canvas.height;
      this.size = Math.random() * 2 + 0.5;
      this.speedX = (Math.random() - 0.5) * 0.4;
      this.speedY = (Math.random() - 0.5) * 0.4;
    }
    update() {
      this.x += this.speedX;
      this.y += this.speedY;
      if (this.x < 0 || this.x > canvas.width) this.speedX *= -1;
      if (this.y < 0 || this.y > canvas.height) this.speedY *= -1;
    }
    draw() {
      ctx.beginPath();
      ctx.arc(this.x, this.y, this.size, 0, Math.PI * 2);
      ctx.fillStyle = "rgba(255,255,255,0.8)";
      ctx.fill();
    }
  }

  function init() {
    particles = [];
    for (let i = 0; i < 100; i++) particles.push(new Particle());
  }

  function animate() {
    ctx.clearRect(0, 0, canvas.width, canvas.height);
    particles.forEach((p) => {
      p.update();
      p.draw();
    });
    requestAnimationFrame(animate);
  }

  window.addEventListener("resize", () => {
    canvas.width = innerWidth;
    canvas.height = innerHeight;
    init();
  });

  init();
  animate();
</script>
