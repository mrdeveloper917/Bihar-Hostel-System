<% title = "Contact Us" %>

<section class="contact-section py-5">
  <div class="container">
    <div class="text-center mb-5">
      <h2 class="fw-bold text-gradient mb-2 animate__animated animate__fadeInDown">
        📬 Contact Us
      </h2>
      <p class="text-muted fs-5 animate__animated animate__fadeInUp">
        Have a question or need help? We’d love to hear from you.
      </p>
    </div>

    <div class="row g-4 align-items-stretch">
      <!-- Contact Form -->
      <div class="col-md-6">
        <div class="card border-0 shadow-sm glass-effect h-100 p-4">
          <h5 class="fw-semibold text-primary mb-3">
            <i class="bi bi-envelope-paper me-2"></i> Send us a message
          </h5>

          <form method="POST" action="/contact" class="needs-validation" novalidate>
            <div class="mb-3">
              <label class="form-label fw-semibold">Name</label>
              <input name="name" class="form-control" placeholder="Your name" required />
              <div class="invalid-feedback">Please enter your name</div>
            </div>

            <div class="mb-3">
              <label class="form-label fw-semibold">Email</label>
              <input type="email" name="email" class="form-control" placeholder="you@example.com" required />
              <div class="invalid-feedback">Please enter a valid email</div>
            </div>

            <div class="mb-3">
              <label class="form-label fw-semibold">Message</label>
              <textarea name="message" rows="5" class="form-control" placeholder="Write your message here..." required></textarea>
              <div class="invalid-feedback">Please enter your message</div>
            </div>

            <div class="d-grid">
              <button class="btn btn-primary rounded-pill py-2 fw-semibold" type="submit">
                <i class="bi bi-send-fill me-1"></i> Send Message
              </button>
            </div>
          </form>
        </div>
      </div>

      <!-- Office Info -->
      <div class="col-md-6">
        <div class="card border-0 shadow-sm glass-effect h-100 p-4 d-flex flex-column justify-content-center">
          <h5 class="fw-semibold text-success mb-3">
            <i class="bi bi-building me-2"></i> Our Office
          </h5>
          <p class="text-muted mb-2">
            Bihar Polytechnic College<br />
            123 Hostel Lane, Patna, Bihar - 800001
          </p>
          <p class="text-muted mb-1">
            <i class="bi bi-envelope me-2"></i> support@biharhostel.local
          </p>
          <p class="text-muted">
            <i class="bi bi-telephone me-2"></i> +91 98765 43210
          </p>
          <div class="mt-4">
            <iframe
              class="w-100 rounded"
              height="230"
              style="border: 0"
              loading="lazy"
              allowfullscreen
              src="https://www.google.com/maps?q=patna&output=embed"
            ></iframe>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<!-- Style -->
<style>
  .text-gradient {
    background: linear-gradient(90deg, #007bff, #6610f2);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
  }

  .glass-effect {
    background: rgba(255, 255, 255, 0.85);
    backdrop-filter: blur(10px);
    border: 1px solid rgba(255, 255, 255, 0.3);
    border-radius: 15px;
    transition: all 0.3s ease;
  }

  .glass-effect:hover {
    transform: translateY(-4px);
    box-shadow: 0 10px 25px rgba(0, 0, 0, 0.1);
  }

  .contact-section {
    background: linear-gradient(180deg, #f8f9fa 0%, #eef2ff 100%);
  }

  .form-control {
    border-radius: 10px;
    padding: 10px 14px;
  }

  .form-control:focus {
    border-color: #007bff;
    box-shadow: 0 0 0 0.15rem rgba(0, 123, 255, 0.25);
  }

  button[type="submit"] {
    transition: all 0.3s ease;
  }

  button[type="submit"]:hover {
    transform: translateY(-2px);
    box-shadow: 0 5px 12px rgba(0, 123, 255, 0.3);
  }

  @media (max-width: 768px) {
    iframe {
      height: 200px;
    }
  }
</style>

<script>
  // Bootstrap form validation
  (() => {
    'use strict';
    const forms = document.querySelectorAll('.needs-validation');
    Array.from(forms).forEach(form => {
      form.addEventListener('submit', event => {
        if (!form.checkValidity()) {
          event.preventDefault();
          event.stopPropagation();
        }
        form.classList.add('was-validated');
      }, false);
    });
  })();
</script>
